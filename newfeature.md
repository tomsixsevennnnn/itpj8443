---
noteId: "bb5ee4708fef11f1a0b1b1af0447d59d"
tags: []

---

# Feature แผน: ต้นทุน/ราคาขายต่อจาน

> สถานะ: **พัฒนาเสร็จแล้ว** ✅ — backend (Prisma schema + migration + DTO/service) และ frontend (staffing.ts, Menus/Settings/Orders/Documents/Dashboard) แก้ตามแผนนี้ครบแล้ว typecheck + test ผ่านทั้งสองฝั่ง

## ที่มา

เจ้าของร้านต้องการรู้ต้นทุนจริงต่องาน ไม่ใช่แค่จำนวนพนักงานที่ต้องใช้ (ซึ่งระบบมีอยู่แล้ว) แต่อยากรู้ด้วยว่า:
1. แต่ละเมนูมีต้นทุนเท่าไหร่ ขายเท่าไหร่ กำไรต่อจานเท่าไหร่
2. ค่าแรงคนแต่ละตำแหน่งรวมแล้วเท่าไหร่ (มีข้อมูลอัตราค่าแรงอยู่แล้ว จะกรอกให้ทีหลัง)

## ข้อสรุปจากการคุยขอบเขต

| ประเด็น | ข้อสรุป |
|---|---|
| ต้นทุนเมนูมาจากไหน | **กรอกตรงๆ ต่อเมนู** — เจ้าของร้านกรอก "ราคาทุน" และ "ราคาขาย" ต่อจานเอง ไม่ต้องมีคลังวัตถุดิบ ไม่ต้องมีสูตรอาหาร ไม่ต้องคำนวณจากวัตถุดิบ |
| อาหารโต๊ะจีน 1 เมนู เสิร์ฟกี่จานต่อโต๊ะ | **1 จานต่อ 1 โต๊ะ** (มาตรฐานโต๊ะจีน) — จำนวนจานที่ต้องทำ = จำนวนโต๊ะ |
| จุดแสดงผลต้นทุน | **ทั้งหมด**: หน้าเมนู (ต้นทุน/กำไรต่อจาน), หน้ารายละเอียดการจอง/เอกสาร (ต้นทุนรวมต่องาน), แดชบอร์ด (สรุปกำไรรวม) |
| อัตราค่าแรง | **Flat rate ต่อคน ต่อตำแหน่ง** (ไม่ใช่รายชั่วโมง) — เจ้าของร้านกรอกเองภายหลัง แยกตามตำแหน่ง (เสิร์ฟ/พ่อครัว/ผู้ช่วย/ล้างจาน) |

## ข้อมูลที่ต้องเพิ่ม

### 1. ต้นทุน/ราคาขายต่อเมนู
ผูกกับ `MenuItem` ที่มีอยู่แล้ว เพิ่ม 2 ช่องกรอกในหน้าแก้ไขเมนู:
- ราคาทุนต่อจาน (บาท) — กรอกตรงๆ


ไม่มีคลังวัตถุดิบ ไม่มีสูตรอาหาร ไม่ต้องคำนวณจากปริมาณวัตถุดิบใดๆ

### 2. เกณฑ์คำนวณจำนวนพนักงาน (แก้ไข — ยืนยันรอบล่าสุดจากผู้ใช้)

| ตำแหน่ง | เกณฑ์ headcount | เทียบกับ `staffing.ts` เดิม |
|---|---|---|
| พนักงานเสิร์ฟ | 1 คน ต่อ 8 โต๊ะ (เกณฑ์เดียว flat) | **เปลี่ยน** — เดิมมีช่วง 5–8 โต๊ะ/คน (`serversMin`/`serversMax`), ตอนนี้ใช้ 8 โต๊ะ/คนเกณฑ์เดียว ต้องลบ/เลิกใช้ `TABLES_PER_SERVER_MIN` และ field `serversMin`/`serversMax` |
| พ่อครัว | 1 คน ต่อ 1 งาน | เหมือนเดิม ไม่เปลี่ยน |
| ผู้ช่วยพ่อครัว | 1 คน ต่อ 20 โต๊ะ · เศษเกิน 10 โต๊ะ +1 คน | เหมือนเดิม ไม่เปลี่ยน (ยืนยันแล้วว่าเป็น 20 ไม่ใช่ 15 ที่เคยให้มาผิด) |
| พนักงานล้างจาน | 1 คน ต่อ 20 โต๊ะ · เศษเกิน 10 โต๊ะ +1 คน | เหมือนเดิม ไม่เปลี่ยน |

### 3. อัตราค่าแรง (Wage Rate)
เพิ่มในหน้า "ตั้งค่า" — แยกตามตำแหน่ง อัตราจริงจากผู้ใช้ (flat ต่อคน) — **ยืนยันครบแล้ว**:

| ตำแหน่ง | อัตรา |
|---|---|
| พ่อครัว | 1,200 บาท / คน |
| ผู้ช่วยพ่อครัว | 1,000 บาท / คน |
| พนักงานเสิร์ฟ | 100 บาท / โต๊ะ / คน |
| พนักงานล้างจาน | 500 บาท / คน |

## สูตรคำนวณ

```
กำไรต่อจาน (เมนู X)
  = ราคาขายต่อจาน − ราคาทุนต่อจาน

ต้นทุนเมนูรวมต่องาน
  = Σ (ราคาทุนต่อจานของแต่ละเมนูที่ลูกค้าเลือก) × จำนวนโต๊ะ
    (เพราะ 1 จาน/โต๊ะ ตามข้อสรุปด้านบน)

ค่าแรงรวมต่องาน
  = ค่าแรงพ่อครัว + ค่าแรงผู้ช่วยพ่อครัว + ค่าแรงเสิร์ฟ + ค่าแรงล้างจาน

  ค่าแรงพ่อครัว      = 1,200 × chefs
  ค่าแรงผู้ช่วยพ่อครัว = 1,000 × assistants
  ค่าแรงล้างจาน       = 500   × dishwashers
  ค่าแรงเสิร์ฟ        = 100   × จำนวนโต๊ะ    (อัตราคิดต่อโต๊ะโดยตรง ไม่ใช่ 100 × servers)

  จำนวนคน (chefs/assistants/dishwashers) ใช้ staffActual ถ้ามี (เจ้าของร้านปรับแล้ว)
  ไม่งั้นใช้ staffAuto (ระบบคำนวณอัตโนมัติจาก staffing.ts ตามเกณฑ์ headcount ข้อ 2 ด้านบน)

ต้นทุนรวมต่องาน
  = ต้นทุนเมนูรวม + ค่าแรงรวม

กำไรต่องาน
  = ราคาขาย (totalPrice) − ต้นทุนรวมต่องาน
```

## จุดที่จะแสดงผล

1. **หน้าเมนูอาหาร (owner-menus)** — การ์ดเมนูแต่ละใบโชว์ราคาทุน/ราคาขาย/กำไรต่อจาน
2. **หน้ารายละเอียดการจอง / เอกสาร (owner-orders, owner-documents)** — สรุปต้นทุนเมนู + ค่าแรง + กำไรของงานนั้น
3. **แดชบอร์ด (owner-dashboard)** — การ์ด/กราฟสรุปกำไรรวมในแต่ละช่วงเวลา (ยอดขาย − ต้นทุนเมนู − ค่าแรง)

## ผลกระทบต่อโครงสร้างระบบ

### Backend (Prisma schema เพิ่ม)
- `MenuItem` เพิ่ม field: `costPrice` (ราคาทุนต่อจาน), `sellPrice` (ราคาขายต่อจาน) — เลขทศนิยม ไม่บังคับกรอก (nullable) เผื่อบางเมนูยังไม่กรอก
- `Settings` เพิ่ม field ค่าแรง 4 ตำแหน่ง (servers/chefs/assistants/dishwashers wage rate)
- ไม่ต้องมี `Ingredient`, `RecipeItem`, หรือ endpoint คลังวัตถุดิบใดๆ

### แก้ `staffing.ts`
- ลบเกณฑ์ช่วง 5–8 โต๊ะ/คนของเสิร์ฟ ใช้เกณฑ์เดียว 8 โต๊ะ/คน แทน
- เอา `TABLES_PER_SERVER_MIN`, `serversMin`, `serversMax` ออก (ไม่ใช้แล้ว) — เช็คก่อนว่าฝั่ง UI ที่ไหนอ้างอิง field พวกนี้อยู่บ้าง (เช่นหน้าที่โชว์ช่วงจำนวนเสิร์ฟ) แล้วปรับตาม
- `TABLES_PER_SUPPORT = 20` (ผู้ช่วยพ่อครัว/ล้างจาน) และ `chefs = 1` คงเดิม ไม่ต้องแก้

### Frontend
- แก้ modal แก้ไขเมนูใน `Menus.tsx` เพิ่ม 2 ช่องกรอก: ราคาทุน / ราคาขาย ต่อจาน
- เพิ่มการ์ดต้นทุน/กำไรใน `Dashboard.tsx`, `Documents.tsx`, `Orders.tsx`
- เพิ่มฟอร์มค่าแรงใน `Settings.tsx`
- ปรับจุดที่แสดงช่วงจำนวนเสิร์ฟ 5–8 โต๊ะ/คน (ถ้ามี) ให้เหลือค่าเดียว 8 โต๊ะ/คน

## รอก่อนเริ่มพัฒนา

- ~~อัตราค่าแรงจริงแต่ละตำแหน่ง~~ ✅ ได้รับแล้ว ครบทุกตำแหน่ง (ดูตารางในหัวข้อ "อัตราค่าแรง")
- ~~เกณฑ์ headcount~~ ✅ ยืนยันแล้ว (ดูหัวข้อ "เกณฑ์คำนวณจำนวนพนักงาน")
- ยืนยันรอบสุดท้ายว่าพร้อมเริ่มลงมือทำ
