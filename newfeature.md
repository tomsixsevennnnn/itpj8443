---
noteId: "bb5ee4708fef11f1a0b1b1af0447d59d"
tags: []

---

# Feature: ต้นทุน/ราคาขายต่อจาน

> สถานะ: **พัฒนาเสร็จแล้ว** ✅ — เอกสารนี้อัปเดตให้ตรงกับโค้ดปัจจุบัน (ดู `costing.ts`, `staffing.ts`, `schema.prisma`)

## ที่มา

เจ้าของร้านต้องการรู้ต้นทุนจริงต่องาน ไม่ใช่แค่จำนวนพนักงานที่ต้องใช้ (ซึ่งระบบมีอยู่แล้ว) แต่อยากรู้ด้วยว่า:
1. แต่ละเมนูมีต้นทุนเท่าไหร่
2. ค่าแรงคนแต่ละตำแหน่งรวมแล้วเท่าไหร่
3. งานแต่ละงานกำไรเท่าไหร่ (เทียบกับราคาที่ตกลงขายจริง)

## ข้อสรุปจากการคุยขอบเขต (และจุดที่ปรับระหว่างพัฒนา)

| ประเด็น | ข้อสรุป |
|---|---|
| ต้นทุนเมนูมาจากไหน | **กรอกตรงๆ ต่อเมนู** — เจ้าของร้านกรอก "ราคาทุน" ต่อจานเอง ไม่ต้องมีคลังวัตถุดิบ ไม่ต้องมีสูตรอาหาร |
| ราคาขายต่อเมนู | **ไม่มี** — ตัดออกระหว่างพัฒนา (`Remove sellPrice from menu items`) เพราะแพ็กเกจต่อรองราคาได้ ผลรวมราคาขายต่อเมนูจึงไม่ตรงกับราคาที่ตกลงขายจริงเสมอไป ใช้ `booking.totalPrice` (ราคาที่ตกลงขายจริงทั้งงาน) แทน |
| กำไรคำนวณระดับไหน | **ระดับงาน (booking)** ไม่ใช่ระดับเมนู — กำไร = `totalPrice` ของงานนั้น ลบต้นทุนรวมของงานนั้น |
| อาหารโต๊ะจีน 1 เมนู เสิร์ฟกี่จานต่อโต๊ะ | **1 จานต่อ 1 โต๊ะ** (มาตรฐานโต๊ะจีน) — จำนวนจานที่ต้องทำ = จำนวนโต๊ะ |
| จุดแสดงผลต้นทุน | หน้าเมนู (ต้นทุนต่อจาน), หน้ารายละเอียดการจอง/เอกสาร (ต้นทุนรวม+กำไรต่องาน), แดชบอร์ด/รายงาน/ลูกค้า (สรุปกำไรรวม) |
| อัตราค่าแรง | **Flat rate ต่อคน ต่อตำแหน่ง** (เสิร์ฟคิดต่อโต๊ะ ไม่ใช่ต่อหัวคน) — แก้ได้จากหน้า "ตั้งค่า" |

## ข้อมูลที่เพิ่มเข้าระบบ

### 1. ต้นทุนต่อเมนู
`MenuItem.costPrice` (Int, nullable) — กรอกตรงๆ ในหน้าแก้ไขเมนู ไม่บังคับกรอก

ไม่มีคลังวัตถุดิบ ไม่มีสูตรอาหาร ไม่ต้องคำนวณจากปริมาณวัตถุดิบใดๆ

### 2. เกณฑ์คำนวณจำนวนพนักงาน (`staffing.ts`)

| ตำแหน่ง | เกณฑ์ headcount |
|---|---|
| พนักงานเสิร์ฟ | 1 คน ต่อ 8 โต๊ะ (เกณฑ์เดียว flat — `TABLES_PER_SERVER = 8`) |
| พ่อครัว | 1 คน ต่อ 1 งาน |
| ผู้ช่วยพ่อครัว | 1 คน ต่อ 20 โต๊ะ · เศษเกิน 10 โต๊ะ +1 คน (`TABLES_PER_SUPPORT = 20`, `REMAINDER_THRESHOLD = 10`) |
| พนักงานล้างจาน | เกณฑ์เดียวกับผู้ช่วยพ่อครัว |

### 3. อัตราค่าแรง (Wage Rate)

เก็บใน `Settings` (ต่อร้าน) แก้ได้จากหน้า "ตั้งค่า" ฝั่งเจ้าของร้าน ค่าเริ่มต้น (`costing.ts`):

| ตำแหน่ง | field ใน Settings | อัตราเริ่มต้น |
|---|---|---|
| พ่อครัว | `wageChef` | 1,200 บาท / คน / งาน |
| ผู้ช่วยพ่อครัว | `wageAssistant` | 1,000 บาท / คน / งาน |
| พนักงานเสิร์ฟ | `wageServerPerTable` | 100 บาท / โต๊ะ |
| พนักงานล้างจาน | `wageDishwasher` | 500 บาท / คน / งาน |

## สูตรคำนวณ (`costing.ts`)

```
ต้นทุนเมนูรวมต่องาน (menuCostFor)
  = Σ (ราคาทุนต่อจานของแต่ละเมนูที่ลูกค้าเลือก) × จำนวนโต๊ะ
    (เพราะ 1 จาน/โต๊ะ ตามข้อสรุปด้านบน)

ค่าแรงรวมต่องาน (wageCostFor)
  = wageChef × chefs + wageAssistant × assistants
    + wageServerPerTable × จำนวนโต๊ะ + wageDishwasher × dishwashers

  จำนวนคน (chefs/assistants/dishwashers) ใช้ booking.staffActual ถ้ามี (เจ้าของร้านปรับแล้ว)
  ไม่งั้นใช้ค่าที่คำนวณอัตโนมัติจาก staffing.ts (calculateStaff → toPlan)

ต้นทุนรวมต่องาน (totalCost)
  = menuCost + wageCost

กำไรต่องาน (profit)
  = booking.totalPrice − totalCost
    (totalPrice คือราคาที่ตกลงขายจริงของงานนั้น ไม่ใช่ผลรวมราคาขายต่อเมนู)
```

## จุดที่แสดงผลจริง

1. **หน้าเมนูอาหาร (`Menus.tsx`)** — การ์ดเมนูแต่ละใบโชว์ราคาทุนต่อจาน
2. **หน้ารายละเอียดการจอง (`Orders.tsx`)** — สรุปต้นทุนเมนู + ค่าแรง + กำไรของงานนั้น
3. **หน้าเอกสาร (`Documents.tsx`)** — สรุปต้นทุน/กำไรของงาน (ไม่พิมพ์ลงในเอกสารที่ให้ลูกค้า)
4. **แดชบอร์ด (`Dashboard.tsx`)** — การ์ดกำไรเดือนนี้ + % เทียบเดือนก่อน
5. **รายงาน (`Reports.tsx`)** — กราฟ/ตารางรายได้-ต้นทุน-กำไรรายเดือน/รายปี
6. **ลูกค้า (`Customers.tsx`)** — สรุปรายได้/ต้นทุน/กำไรสะสมต่อลูกค้า

ทุกจุดใช้ฟังก์ชันกลางเดียวกัน `bookingCostSummary()` ใน `costing.ts`

## โครงสร้างระบบที่เกี่ยวข้อง

### Backend (`schema.prisma`)
- `MenuItem.costPrice` — ราคาทุนต่อจาน (Int, nullable) — **ไม่มี `sellPrice`**
- `Settings.wageChef` / `wageAssistant` / `wageServerPerTable` / `wageDishwasher` — อัตราค่าแรง 4 ตำแหน่ง
- ไม่มี `Ingredient`, `RecipeItem`, หรือ endpoint คลังวัตถุดิบใดๆ

### Frontend
- `src/costing.ts` — ฟังก์ชันคำนวณต้นทุน/ค่าแรง/กำไรกลาง (`menuCostFor`, `wageCostFor`, `bookingCostSummary`)
- `src/staffing.ts` — เกณฑ์คำนวณกำลังคน (เสิร์ฟ 8 โต๊ะ/คนเกณฑ์เดียว ไม่มี `serversMin`/`serversMax`/`TABLES_PER_SERVER_MIN` แล้ว)
- `Menus.tsx` — ฟอร์มกรอกราคาทุนต่อจาน
- `Settings.tsx` — ฟอร์มแก้อัตราค่าแรง 4 ตำแหน่ง
- `Dashboard.tsx`, `Documents.tsx`, `Orders.tsx`, `Reports.tsx`, `Customers.tsx` — แสดงผลต้นทุน/กำไรจาก `bookingCostSummary()`
